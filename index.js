const chalk = require('chalk');

console.log(
    'It is ' + `${chalk.blue('MMMM Do YYYY, h:mm:ss a')}.
  `);


// moment().format(chalk.blue('MMMM Do YYYY, h:mm:ss a'));
// moment().format(chalk.magenta('dddd'));
